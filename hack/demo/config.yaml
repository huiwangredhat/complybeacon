plugins:
  - id: conforma
    evaluations-dir: "/sampledata/evaluations"

certConfig:
  cert: /certs/compass.crt
  key: /certs/compass.key

# Add this block to control JWT
jwtAuth:
  enabled: true
  # enabled: false
